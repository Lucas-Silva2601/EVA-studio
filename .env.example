# EVA Studio – Variáveis de ambiente (exemplo)
# Copie este arquivo para .env.local e preencha com suas chaves.
# O arquivo .env.local não é versionado (está no .gitignore).

# Chave da API Groq (obrigatória para o Agente Analista)
# Obtenha em: https://console.groq.com/keys
GROQ_API_KEY=
